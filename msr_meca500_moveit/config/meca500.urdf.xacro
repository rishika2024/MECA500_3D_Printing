<?xml version="1.0"?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro" name="meca500">

    <!-- use_mock_hardware:=true  → mock_components/GenericSystem (default, no real robot needed) -->
    <!-- use_mock_hardware:=false → msr_meca500_hardware/Meca500System (real robot at robot_ip)     -->
    <xacro:arg name="use_mock_hardware" default="true"/>
    <xacro:arg name="robot_ip"          default="192.168.0.100"/>
    <xacro:arg name="control_port"      default="10000"/>
    <xacro:arg name="monitoring_port"   default="10001"/>
    <xacro:arg name="use_extruder"      default="true"/>

    <xacro:include filename="$(find msr_meca500_robot)/urdf/robot.urdf.xacro" />

</robot>
